// Sie s un proyecto que ya existia, con este comando vuelve a crear los modulos que existen en el json
npm i
----------------------------------------------------------------------------------------------------------
//crear el json
npm init -y

// install expressy ejs
npm i express ejs


//instalamos nodemon
npm i nodemon -D

//para instalar mongoose
npm install mongoose

//Instala la libreria que permite usar las variables de entorno
npm i dotenv
--------------------------------------------------------------------------------------------------------------
//esto va en los scripts del jason
"start": "nodemon app.js",

// deja usar el dev pra arrancar
"dev": npm run dev

